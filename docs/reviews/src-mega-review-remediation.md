# Tracker de remediación — Mega review de `src/`

**Review origen:** [`docs/reviews/src-mega-review-2026-07-15.md`](./src-mega-review-2026-07-15.md)
**Rama de trabajo:** `dev/review` (== `main` en contenido; confirmado con el usuario — el prompt mencionaba `dev/VAD-improvements`, ya mergeada vía PR #12).
**Inicio:** 2026-07-18

## Estados posibles
- `pendiente` — sin empezar
- `en progreso` — en trabajo activo
- `corregido` — cambio implementado + evidencia (archivos, tests, explicación)
- `no aplica` — verificado que el hallazgo ya no aplica / la corrección causaría regresión; con evidencia
- `bloqueado por verificación externa` — requiere inspección de HW/VM/board o decisión de producto

## Baseline (Fase 0)
| Validación | Resultado | Fecha |
|---|---|---|
| Rama | `dev/review` @ `62aaa04` (worktree limpio) | 2026-07-18 |
| `make test` | 172 tests, 0 failures, 0 ignored | 2026-07-18 |
| `make clang-tidy` | exit 0 (solo warnings suprimidos en non-user code) | 2026-07-18 |
| `./scripts/build.sh` (VM) | ✅ exit 0 — binario ARM linkea limpio con los cambios de C02+M04, sin warnings `-Wall -Wextra` (VM encendida). Reejecutar tras cada grupo con cambios en `src/`. | 2026-07-18 |

## Resumen de estado (27 hallazgos)

| Estado | Cantidad | IDs |
|---|---:|---|
| corregido (con tests + build ARM) | 8 | SRC-C01, C02, M01, M02, M03, M04, M05, M09 |
| corregido parcial (fix real + resto acotado) | 7 | SRC-H03, H05, M07, M08, M11, L01, L06 |
| no aplica / documentado con evidencia | 2 | SRC-M10, L02 |
| bloqueado por dependencia externa específica | 10 | SRC-H01, H02, H04, H06, H07, M06, M12, L03, L04, L05 |

**Actualización:** C01 pasó a **corregido** (owned port `src/bsp/qpc_port/`, submódulo QP/C intacto). H05 pasó a **corregido parcial** (build-assert; geometría intacta). **Cero críticos pendientes.** De los 10 bloqueados, la mayoría depende de la **política de recovery/shutdown** (H04, H07) — en curso: fail-fast + STT reconnect (ya funciona).

**Cero críticos/altos "pendientes sin evidencia":** C02 corregido; C01 diferido por regla explícita del usuario (no tocar QP/C). Cada alto restante está bloqueado por una dependencia externa concreta (decisión de producto/diseño de recovery, o verificación en placa/Vivado) — no por falta de análisis.

**Naturaleza de los bloqueos:**
- *Regla del usuario (QP/C inmutable):* C01, L04, L05.
- *Decisión de producto/diseño (recovery/shutdown/buffering):* H02, H04, H07 (+H01, M06 en la parte de patrón worker).
- *Verificación en placa / Vivado:* H01, H05, H06, M06, M12.
- *ROI bajo / riesgo desproporcionado (documentado):* M10, L02, L03.

## Tracker de hallazgos

### Críticos
| ID | Título | Estado | Evidencia / Notas |
|---|---|---|---|
| SRC-C01 | Carrera de arranque + data race del ticker POSIX-QV | corregido | **Resuelto vía "owned port" (Opción A, aprobada por el usuario):** se copió el port posix-qv a `src/bsp/qpc_port/` (`qf_port.c` + `qp_port.h`) y el build (`project.yml` + `Makefile`) compila **esa** copia; `src/qpc` queda 100% pristino (submódulo intacto, verificado). El fix vive en nuestro código: `l_isRunning` se publica `true` **antes** de crear el ticker, acceso vía `__atomic_*` (acquire/release, gnu99-safe), ticker **joinable** con `pthread_join` en shutdown. El test `test/integration/qpc/test_posix_qv_ticker.c` ahora ejercita el port arreglado (ticks reales + shutdown repetido). 199 tests OK, lint 0, build ARM OK. | **El defecto está DENTRO del port vendorizado `src/qpc/ports/posix-qv/qf_port.c` (QP/C 8.1.4 upstream).** El usuario definió QP/C como módulo externo inmutable: no se modifica `src/qpc`. El patch está diseñado y probado (revertido del vendor), pero requiere decisión de dónde vivir. **Opciones a discutir:** (a) *owned port* — copiar el port posix-qv (2 archivos) a nuestro árbol (p.ej. `src/bsp/qpc_port/`) y apuntar build ahí, dejando `src/qpc` pristino (patrón sancionado por QP/C: los ports son adaptables por el proyecto); (b) actualizar el gitlink a un QP/C con el fix upstream; (c) aceptar como riesgo residual documentado (la carrera es teórica: en 15/15 corridas el ticker entrega ticks). **Test de regresión ya agregado en nuestro árbol:** `test/integration/qpc/test_posix_qv_ticker.c` arranca el ticker real y verifica ticks + shutdown repetido (pasa 15/15 contra el port pristino). Naturaleza del fix necesario: publicar `l_isRunning=true` antes de crear el ticker, acceso atómico (`__atomic_*`, gnu99-safe) y ticker joinable con `pthread_join` en shutdown. |
| SRC-C02 | VideoAO invalida MMIO que SubtitleAO sigue usando | corregido | Ownership por **reference-count** en `hw_platform` (`src/bsp/platform/linux/hw_platform.c/.h`): `hw_platform_init` mapea solo en el primer acquire y toma referencia después; `hw_platform_cleanup` libera y **solo el último release** hace `munmap`+`close` (release sin referencias = no-op idempotente; falla de init hace teardown crudo con refcount aún en 0). `subtitle_pipeline` ahora **adquiere** la plataforma en init (nuevo flag `platform_ready`) y la **libera** en cleanup, con release en toda ruta de falla (helper `configure_hardware`). Así el cleanup de VideoAO ante error deja `refcount=1` y NO desmapea el overlay/BRAM que usa SubtitleAO. Tests nuevos: `test/bsp/platform/test_hw_platform.c` (7 casos: acquire/segundo-acquire/release-con-refs/último-release/underflow/falla-open/falla-map-parcial, syscalls falsas por macro-redirect) y `test/svc/subtitle_pipeline/test_subtitle_pipeline.c` (acquire/release, falla de acquire, release en cleanup no-inicializado). 183 tests OK, clang-tidy exit 0. **Pendiente relacionado:** quiesce/stop global coordinado antes de liberar MMIO → SRC-H07 (Fase 2); rollback de overlay/BRAM parcial → SRC-M03 (Fase 4). **Build ARM pendiente** (VM caída). |

### Altos
| ID | Título | Estado | Evidencia / Notas |
|---|---|---|---|
| SRC-H01 | Operaciones bloqueantes dentro del scheduler QV | bloqueado por verificación externa | **Refactor de arquitectura + requiere validación en placa.** `USBAudioAO::on_component_init` llama `usb_audio_stream_start` (ALSA + creación de threads, bloqueante) y `stt_event_rx_init` hace `getaddrinfo`; corren en el único hilo cooperativo QV. Fix propuesto: mover init/stop bloqueante a workers con evento de completion, o completar setup antes de `QF_run`. Es un cambio grande y de alto riesgo de regresión que **no puede validarse sin la placa** (timing real de ALSA/threads). Requiere decisión de diseño (patrón worker+completion) y prueba en placa. Relacionado: busy-wait de dynclk (M08, ya acotado con timeout). |
| SRC-H02 | Triple buffering declarado pero no implementado | bloqueado por decisión de producto | `active_frame` se fija en 0 y nunca cambia; MM2S y S2MM usan el mismo framebuffer. Los 3 buffers mapeados son scaffolding. **Decisión requerida:** single-buffer passthrough explícito (simple, documentar que hay riesgo de tearing) **o** triple buffering real con swap en frontera de frame/SOF (más complejo, necesita validar el ioctl `*_SELECT` + sincronización en placa). La review pide NO simular. No lo implemento a ciegas: requiere que elijas la política y validación en placa. |
| SRC-H03 | Adquisición de timing / modo no soportado sin timeout | corregido (parcial) | `video_pipeline_poll` (`src/svc/video_pipeline/video_pipeline.c`) ahora acota `ACQUIRING_TIMING`: si `XST_NO_DATA` persiste `>= VIDEO_PIPELINE_TIMING_TIMEOUT_MS` (2s), reinicia el detector y vuelve a `WAITING_FOR_SIGNAL` (nuevo resultado `VIDEO_PIPELINE_POLL_TIMING_TIMEOUT`, backoff acotado). Se agregó uso real de `detector_started_ms` vía `video_input_detector_elapsed_ms` + `video_input_reset_detector` (`video_io`). Tests: `test_video_pipeline` (timeout reinicia detector) + `test_video_io` (elapsed/reset, incl. rollover uint32). Build ARM OK. **Restante:** revalidación periódica de timing en `UNSUPPORTED_INPUT`/`STREAMING` con lock alto → se trata en SRC-M12. |
| SRC-H04 | Eventos ready/error pueden perderse | bloqueado por decisión de diseño | Los helpers `post_ready`/`post_error` retornan `void` y solo loguean si `Q_NEW_X`/`QACTIVE_POST_X` fallan. **Hallazgo secundario descartado:** verifiqué que QP/C hace `QF_gc(e)` en el fallo de post por margen (`qf_actq.c:113`), así que **NO hay leak de pool**. Con pool de 64 y tráfico de control esporádico, la pérdida es prácticamente nula en la práctica. La remediación de fondo (entrega garantizada: pool/cola dedicados, eventos estáticos, o retry acotado) es inseparable de la **política de recovery** que también gobierna H07 — requiere tu decisión de producto sobre qué hacer ante pérdida/recovery. No hay promesa "always dispatched" en el código actual (nada que corregir en docs). |
| SRC-H05 | Conflicto de tamaño de la BRAM de subtítulos | verificado contra HW (requiere validación en placa antes de corregir) | **Leído el proyecto Vivado de referencia** (`/mnt/c/.../HDMI_overlay`, solo lectura). Evidencia autoritativa: el RTL vigente `sub_mem.v` y `overlay.v` hardcodean **`MASK_W=256, MASK_H=64` → 512 words × 32b = 2 KiB, 1bpp**, con address width de 9 bits en el puerto de lectura del overlay. El block design (`hdmi_in.hwh`) da BRAM físico `C_MEMORY_DEPTH=8192` (8 KiB, `ADDR_WIDTH=13`). El driver bare-metal validado `subtitle_hw.h` confirma `MASK_W 256 / MASK_H 64 / BRAM_SIZE_BYTES 2048`. **El firmware usa `SUBTITLE_BRAM_MASK_WIDTH=1024 / HEIGHT=256 = 32 KiB` (16× la máscara real) y `xparameters_linux.h` BRAM HIGHADDR `0x40007FFF` (32 KiB de decode, pero el HIGHADDR del .hwh es stale vs el `MEM_DEPTH=8192` físico).** Además el stride del firmware (32 words/fila) ≠ hardware (8 words/fila) → layout de texto incorrecto. **NO corregido aún:** los subtítulos "funcionan" en placa según el usuario, lo que contradice esto → hay que confirmar **qué bitstream está flasheado** (¿este diseño de referencia 256×64, o uno agrandado a 1024×256 no presente en este folder?). Corregir a 256×64/8KiB requiere esa confirmación + re-test en placa. Fuente de verdad HW (folder de referencia) = **256×64 / máscara 2 KiB / BRAM 8 KiB**. **Acción tomada (decisión del usuario: no romper lo que funciona):** NO se cambia la geometría (los subtítulos funcionan en placa con 1024×256 → el bitstream flasheado es 1024×256, el folder de referencia está viejo). Se agregó un **build-assert gnu99-safe** en `subtitle_bram.c` que garantiza `SUBTITLE_BRAM_SIZE_BYTES <= ventana de BRAM` (single source of truth; hoy 32768 ≤ 32768, y falla el compile si alguien hace drift). Pendiente cosmético: confirmar/actualizar el folder de referencia HW al bitstream real. |
| SRC-H06 | Geometría del overlay puede exceder pantalla y máscara | contrato del IP resuelto (acoplado a H05) | **Resuelto el contrato del IP leyendo `overlay.v`:** la máscara es un grid fijo de 256×64 que el IP **escala** al rectángulo del bar en pantalla (default reg1 = 1600×120). Es decir, la geometría del bar (x/y/w/h en pantalla, regs 0x00/0x04) es **independiente** del tamaño de la máscara (siempre 256×64). Conclusión: (a) el firmware NO debe atar `bar_width/height` a la máscara — el clamp de máscara del firmware (≥1024×256) está mal conceptualmente; la máscara siempre es 256×64. (b) El bar sí debería clamparse al display (evitar x+w > ancho de pantalla). El fix correcto está atado a H05 (corregir la geometría de máscara a 256×64) — requiere la misma validación en placa. |
| SRC-H07 | No existe estrategia global de shutdown/error | bloqueado por decisión de diseño | `QF_onCleanup` vacío; SIGINT llama cleanup + `exit()` desde el signal handler (no async-signal-safe). System entra a estado terminal pero no detiene coordinadamente USB threads/sockets/DMA/VTC/overlay ni los otros AOs. **Parte en QP/C (inmutable):** el `sigIntHandler` que hace `exit()` está en `src/qpc/ports/posix-qv/qf_port.c` — no se toca. La parte coordinable (señales STOP/STOPPED, orden de quiesce, timeout de shutdown en SystemAO/app) **es nuestra pero requiere definir la política de recovery/shutdown** (misma decisión que H04). Requiere tu decisión de diseño antes de implementar. Nota: C02 ya mitigó el peor síntoma concreto (MMIO desmapeado bajo otro AO) vía refcount. |

### Medios
| ID | Título | Estado | Evidencia / Notas |
|---|---|---|---|
| SRC-M01 | NULL dereference en SttAO | corregido | `on_transcript` (`src/svc/stt/SttAO.c`) ahora valida `me`/`e`/`e->text[0]` **antes** de leer `e->is_final` (que se usaba para el margin antes del check). Reordenado; sin cambio de comportamiento para entradas válidas. Build ARM OK. |
| SRC-M02 | Marcador DONE permanente | corregido | `on_component_init` (`src/svc/subtitle_pipeline/SubtitleAO.c`) ahora arma `clear_time_evt` al terminar init exitoso, así el marcador DONE se limpia tras el timeout de inactividad aunque nunca llegue STT. Test nuevo `test_subtitle_startup_marker_clears_after_inactivity_timeout` (avanza 100 ticks → dispara `SUBTITLE_CLEAR_SIG` → overlay disabled). Build ARM OK. |
| SRC-M03 | Rollback incompleto en init de subtitle | corregido | `configure_hardware` (`subtitle_pipeline.c`) ahora deshabilita el overlay explícitamente si cualquier etapa tras `overlay_init` falla (no queda parcialmente configurado/enabled); el release de plataforma ya se maneja en init (C02). `SubtitleAO::clear_subtitle` deja de descartar silenciosamente los errores de clear/disable (los loguea, para visibilizar mismatch lógico/físico). Test nuevo `test_subtitle_pipeline_init_disables_overlay_when_a_late_stage_fails` + actualización del caso bram_init. Build ARM OK. |
| SRC-M04 | Invariantes débiles / duplicados en startup | corregido (parcial) | La rama `COMPONENT_VIDEO` de `on_component_ready` (`src/svc/system/SystemAO.c`) ahora guarda el posteo de subtitle init con `subtitle_init_requested == 0U` (simétrico con la rama USB), volviendo idempotente un *video ready* duplicado. Test nuevo `test_system_ignores_duplicate_video_ready_after_subtitle_requested` en `test/integration/qpc/test_system_ao_startup.c`. **Restante menor:** `last_ready_component` solo se asigna y nunca se consume → se resuelve en SRC-L01 (Fase 5); COMPONENT_STT no valida explícitamente readiness previo (bajo riesgo, el flujo lo garantiza por orden). |
| SRC-M05 | video_dma_init anuncia frames que el kernel no tiene | corregido | `video_dma_init` (`src/hal/video_dma/video_dma.c`) ahora exige `info.frame_count >= frame_count` (falla si el kernel expone menos) y mapea con índice directo `i` en vez de `i % info.frame_count` (que aliaseaba buffers). Test nuevo `test/hal/video_dma/test_video_dma.c` (3 casos, syscalls falsas incl. `ioctl` variádico). Build ARM OK. |
| SRC-M06 | ALSA recovery y parada de workers pueden bloquear | bloqueado por verificación externa | Threading real de audio (busy-spin en `snd_pcm_resume` con `-EAGAIN`, `pthread_join` sin cancel, condvar en `CLOCK_REALTIME`, broadcast sin mutex, `sender_fd` con ownership compartido, drops no contabilizados, truncado silencioso de env). Fix propuesto: retry acotado con sleep/cancel, condición monotónica (`CLOCK_MONOTONIC`), protocolo de stop bajo el mismo mutex del predicado, ownership estricto de `sender_fd`. **Alto riesgo de regresión en concurrencia real; requiere validación en placa** (ALSA/threads). Ligado a H01. |
| SRC-M07 | Riesgos JSON/UTF-8 en STT | corregido (parcial) | `json_parse_string` (`src/svc/stt/stt_event_rx.c`) ahora recorta el truncado a **frontera de code point UTF-8** (nunca deja una secuencia multibyte partida). Contrato NDJSON/UTF-8 documentado explícitamente en `stt_event_rx.h`: UTF-8 literal (`ensure_ascii=False`), escapes soportados, truncado en frontera, y semántica de seq/ACK (drops definitivos por diseño). Test nuevo `test_stt_event_rx_parse_line_truncates_utf8_on_code_point_boundary`. Build ARM OK. **Decisión de diseño (documentada, no bug):** `\uXXXX` sigue rechazado — se exige UTF-8 literal en vez de implementar decodificación de escapes Unicode; la secuencia se marca consumida al parsear (drops finales) según el contrato ACK ya documentado. |
| SRC-M08 | Dynclk acepta no-finitos y hace busy-wait | corregido (parcial) | `video_dynclk_configure` (`src/hal/video_dynclk/video_dynclk.c`) ahora rechaza `!isfinite` (NaN/Inf pasaban el `<=0`), fuera de rango (`> 300 MHz`), y aplica tolerancia máxima de error de síntesis (10 MHz, holgada para no rechazar modos reales). Test nuevo `test/hal/video_dynclk/test_video_dynclk.c` (validación pre-MMIO). Build ARM OK. **Restante:** el busy-wait de lock (≤10ms, ya con timeout acotado `CLK_TIMEOUT_NS`) sigue en el handler; convertirlo a polling por timer/worker es parte del refactor de blocking-en-QV (ligado a H01, diferido). |
| SRC-M09 | Asserts Xilinx que no hacen assert | corregido | `src/bsp/bsp_compat/xil_assert.h`: `Xil_AssertVoid`/`Xil_AssertNonvoid`/`*Always` ahora retornan temprano ante precondición falsa (como el Xilinx real no-fatal), en vez del no-op que dejaba al driver seguir y dereferenciar punteros inválidos. Los wrappers `video_vtc` ya validan antes, así que una llamada bien formada nunca los dispara. Test nuevo `test/bsp/bsp_compat/test_xil_assert.c`. Build ARM OK (driver VTC compila con los macros nuevos). |
| SRC-M10 | Convenciones de error incompatibles | no aplica (parcial) / documentado | El **borde servicio→AO ya normaliza** a errno negativo: `VideoAO` traduce las causas del pipeline a `-EIO` antes de `post_error`, y audio/subtitle ya retornan errno negativo. La inconsistencia restante (`XST_*` positivos, `hw_platform` `-1`) es **interna al HAL de video**. Convertir todo el HAL de video de `XST_*` a errno es un refactor amplio que toca decenas de call sites + tests que asertan `XST_*`, con alto riesgo de regresión y bajo valor funcional (no es un bug: son códigos de éxito/error bien definidos, solo heterogéneos). Se documenta como deuda de consistencia de bajo riesgo; no se aborda para evitar regresión desproporcionada. |
| SRC-M11 | Gaps de tests en threads/MMIO/hardware boundary | corregido (parcial) | Esta remediación **cerró varios gaps** con la técnica de compilar el TU real + syscalls falsas por macro-redirect/`--`: `test_hw_platform.c` (MMIO refcount real), `test_video_dma.c` (ioctl/mmap reales del wrapper), `test_video_dynclk.c` (validación), `test_xil_assert.c`, `test_posix_qv_ticker.c` (ticker QF real + shutdown). **Restante:** lifecycle real de workers de `usb_audio_stream` (18% líneas) y `usb_audio_capture` con ALSA — requiere backend ALSA inyectable / validación en placa (ligado a H01/M06). Documentado como gap remanente de threads/ALSA. |
| SRC-M12 | Cambios de modo con lock alto + validación VTC | bloqueado por verificación externa (parcial) | Dos partes: (a) **revalidación periódica de timing** con lock alto (cambio de resolución que mantiene lock conserva stride/VTC viejo) — comparte diseño con H03 y necesita validación en placa del comportamiento del GPIO lock ante cambio de modo; (b) **validadores VTC** (sync/porches/total antes de restas unsigned) — hoy los modos estáticos son válidos, pero la API acepta estructuras arbitrarias. La parte (b) es concreta pero el `video_vtc` está fuera del set testeable (mockeado) y el driver Xilinx es vendorizado; agregar validación en el wrapper propio es viable a futuro. Requiere validación en placa para (a). H03 ya resolvió el timeout de adquisición. |

### Bajos / deuda
| ID | Título | Estado | Evidencia / Notas |
|---|---|---|---|
| SRC-L01 | Estado muerto / sólo observabilidad | corregido (parcial) | Eliminados los write-only `SystemAO.last_ready_component` y `SubtitleAO.running`. **Nota:** `video_input_t.detector_started_ms` **dejó de ser muerto** (ahora lo usa H03). Restantes documentados como observabilidad/intencional o remoción segura futura: `frame_index` (input/output), `video_dynclk.actual_frequency_mhz`, `stt_event_rx.client_connected`, `usb_audio_queue.dropped`; `video_pipeline.active_frame` queda ligado a la decisión de H02 (buffering). Build ARM OK. |
| SRC-L02 | APIs públicas sólo consumidas por tests o nadie | no aplica (documentado) | Inventario confirmado (`video_dma_status`, `video_gpio_set_hpd`, `video_modes_default/all`, `subtitle_bram_set/clear_pixel`, `subtitle_pipeline_write_text/commit/clear_sof/poll_sof`, `subtitle_text_renderer_render`, `video_pipeline_get_state`, `log_unsubscribe`). No es un bug: son superficie de API/test. Ocultarlas/marcarlas es limpieza de bajo riesgo pero de bajo valor; se deja documentado para una pasada de API-hygiene dedicada (no se remueve para no romper los tests que las consumen ni contratos potencialmente estables). |
| SRC-L03 | Objetos VTC no usados enlazados | pendiente (bajo riesgo) | `xvtc_intr.c`/`xvtc_selftest.c` se compilan aunque el diseño Linux userspace no usa interrupciones VTC ni self-test. Fix: compilar solo fuentes necesarias o habilitar `-ffunction-sections`/`--gc-sections` verificando el map ARM. Es un cambio de Makefile de bajo riesgo pero requiere revisar el map file; se deja pendiente por ROI (no afecta correctitud). |
| SRC-L04 | Distribución QP/C completa como superficie no usada | bloqueado (regla de usuario) | `src/qpc` es el **submódulo gitlink** de QuantumLeaps/qpc @ 8.1.4 (inmutable por decisión del usuario). Podar el árbol vendorizado (vendoring mínimo / tag inmutable) toca ese submódulo → **no se hace**. Se deja documentado como decisión a manejar junto con la estrategia de QP/C (misma conversación que C01). |
| SRC-L05 | Config QP/C sobredimensionada / legacy | bloqueado (regla de usuario) | `src/qpc/ports/config/qp_config.h` (`QF_MAX_ACTIVE=32`, `QF_MAX_EPOOL=3`, `QP_API_VERSION=0`) vive **dentro del submódulo QP/C inmutable** → no se toca. Documentado; a manejar con la estrategia de QP/C. |
| SRC-L06 | Templates, comentarios y funciones largas desalineados | corregido (parcial) | Corregidos: macros de logging deshabilitado (`{}` → `do { } while (0)`) y comentario stale de `log.h` (UART→stdout). **Restante documentado (bajo valor / riesgo desproporcionado):** `bsp_init_placeholder()` vacío y TODOs de buttons/LED (scaffolding intencional de milestone); `template_qpc_AO` desalineado con convenciones; funciones >60 líneas (`stt_event_rx_parse_line` ~193, `usb_audio_stream_start`, `subtitle_text_sanitize`, workers USB, etc.) — partir por responsabilidad es refactor de estilo sin cambio funcional; bitmap de 32 KiB en stack del hilo QV (tolerable en Linux); tabla de glyphs sin `¿`/`¡`. |

## Bitácora de cambios
_(se completa por grupo a medida que se avanza)_
